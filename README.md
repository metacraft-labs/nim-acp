# nim-acp

`nim-acp` models the Agent Client Protocol in Nim and provides transport-neutral client helpers for native and JavaScript builds.

Implemented operations:
- `initialize`
- `session/new`
- `session/prompt`
- `session/cancel`
- `session/update` subscription/drain helpers

Development uses the sibling `nim-everywhere` checkout through local `--path` flags.
